<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
            <h4 class="modal-title">Переименовать "@ViewData["item"]"</h4>
        </div>
        <div class="modal-body">
            <form asp-controller="Site" asp-action="CategoryEdit" asp-route-address="@ViewData["address"]" asp-route-item="@ViewData["item"]" asp-route-type="@ViewData["type"]" class="form-inline" role="form">
                <input type="text" class="form-control" name="value" value="@ViewData["item"]" maxlength="70" style="width:100%"/>
                <input type="submit" value="Сохранить" class="btn btn-success" />
            </form>
        </div>
    </div>
</div>


